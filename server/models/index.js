const { Admin } = require('./Admin');
const { Customer } = require('./Customer');
const { Product } = require('./Product');
const { Order } = require('./Order');
const { Review } = require('./Review');

module.export = { Admin, Customer, Product, Order, Review };